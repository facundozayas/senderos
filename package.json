{
  "name": "senderos",
  "private": true,
  "version": "0.1.0",
  "description": "Senderos — PWA vanilla de trekking/bike/running. Este package.json existe solo para correr los tests; la app en si sigue sin dependencias ni build step.",
  "scripts": {
    "test": "node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  },
  "engines": {
    "node": ">=18"
  }
}
